# V6 Archive

Move the former source of truth and other superseded fixed-16-section documents into this folder before implementation changes begin.

Do not delete them until the V3 migration is complete and verified. Add the following header to each archived file:

```text
STATUS: LEGACY — SUPERSEDED BY PROJECT_SOURCE_OF_TRUTH.md V7
USE: Historical implementation context only
```
